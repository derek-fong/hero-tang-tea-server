import * as Date from './date.scalar.graphql';
import * as DateTime from './date-time.scalar.graphql';
import * as EmailAddress from './email-address.scalar.graphql';

export default [Date, DateTime, EmailAddress];
